-record(dstree,
        {id,
         status = initial,
         origin,
         parent,
         neighbors = ordsets:new(),
         children = ordsets:new(),
         subtree = orddict:new(),
         tree,
         waiting_timeout,
         waiting_for,
         timers = orddict:new(),
         dbg_fun,
         report_fun,
         send_fun = fun dstree:default_send/2}).
